{
  "name": "lovable-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack application with React frontend and Express backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && node --loader ts-node/esm index.ts",
    "vercel-build": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

